import Card from "../components/Card";
import Layout from "../components/Layout";

function QuizSettings() {
  return (
    <Layout>
      <Card>
        <h1>Configure Quiz</h1>
        <p>Quiz settings page working.</p>
      </Card>
    </Layout>
  );
}

export default QuizSettings;